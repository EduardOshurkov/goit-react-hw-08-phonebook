import { NavLink } from "react-router-dom";


const NavbarMenu = () => {
    return (
        <div>
            <NavLink to='/contacts'>Contacts</NavLink>
        </div>
    )
};

export default NavbarMenu;